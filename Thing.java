

public class Thing
{
   private String color;
   public Thing(){
       color = "Blue"; 
    }
    public Thing(String setColor){
        color = setColor;
    }
}
/*(a). LightSequence gradShow = new LightSequence("0101 0101 0101"); 
 *(b). gradShow.display();
 *(c). gradShow.changSequence("0011 0011 0011");
 *(d). gradShow.insertSegment("1111 1111",4)
 *(e). public void removeSequence(String oldSeq, String seg){
 *          oldSeqA = oldSeq.subString(0,oldSeq.indexOf(seg));
 *          oldSeqB = oldSeq.subString(oldSeqA.`length()+seg.length()+1,oldSeq.length()+1);
 *          oldSeq = oldSeqA + oldSeqB;
 *    }
 *(f).System.out.println((double)math.sqrt(math.pow(a,2)+math.pow(b,2)));
 */













